---
description: Launch a durable detached OMP agent for a bounded background assignment.
argument-hint: "[assignment]"
---

Read `skill://spawn-detached-agent` and execute it for `$ARGUMENTS`. Apply its isolation, ownership, logging, and lifecycle safeguards. Return the spawned agent identifier, working directory, log location, and exact status command.
